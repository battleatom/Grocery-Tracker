import React,{useEffect,useMemo,useState}from'react';import{createRoot}from'react-dom/client';import{Search,MapPin,RefreshCw,Trophy,CheckCircle2}from'lucide-react';import'./style.css';
const STORE_NAMES=["Sam's Club","Walmart","Albertsons","Safeway","Smith's"];
const money=n=>'$'+Number(n).toFixed(2);
function App(){const[data,setData]=useState(null),[q,setQ]=useState(''),[cat,setCat]=useState('All'),[err,setErr]=useState('');
useEffect(()=>{fetch('/data/prices.json?refresh='+Date.now(),{cache:'no-store',headers:{'Cache-Control':'no-cache'}}).then(r=>{if(!r.ok)throw Error('Price feed unavailable');return r.json()}).then(setData).catch(e=>setErr(e.message))},[]);
const items=data?.items||[];const cats=['All',...new Set(items.map(x=>x.category))];
const shown=useMemo(()=>items.filter(x=>(cat==='All'||x.category===cat)&&x.name.toLowerCase().includes(q.toLowerCase())),[items,cat,q]);
const count=items.reduce((n,x)=>n+x.offers.filter(Boolean).length,0);
return <><header><div className="brand">🛒 <span>Farmington Grocery Watch</span></div><div className="location"><MapPin size={15}/> Farmington, NM</div><div className="updated"><RefreshCw size={13}/> VERIFIED DATA</div></header><main>
<section className="miniStats"><div><b>{items.length}</b><span>foods tracked</span></div><div><b>{count}</b><span>verified prices</span></div><div><b>5</b><span>local stores</span></div></section>
<section className="controls"><div className="search"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search groceries..."/></div><div className="cats">{cats.map(x=><button key={x} className={cat===x?'active':''} onClick={()=>setCat(x)}>{x}</button>)}</div></section>
<section className="panel"><div className="panelhead"><div><h2>Local price comparison</h2><p>Lowest comparable unit price is highlighted. Swipe sideways on mobile.</p></div><span>{shown.length} items</span></div>
{err?<div className="error">{err}</div>:!data?<div className="loading">Loading local prices…</div>:<div className="scroll"><table><thead><tr><th>Item</th>{STORE_NAMES.map(s=><th key={s}>{s}</th>)}<th>Best</th></tr></thead><tbody>{shown.map(item=>{let best=-1,bestUnit=Infinity;item.offers.forEach((o,i)=>{if(o&&o.unit_price<bestUnit){bestUnit=o.unit_price;best=i}});return <tr key={item.id}><td><b>{item.name}</b><small>{item.category} · compare /{item.comparison_unit}</small></td>{item.offers.map((o,i)=><td key={i} className={o?(i===best?'priceCell best':'priceCell'):''}>{o?<><strong>{money(o.price)}</strong><small>{money(o.unit_price)}/{o.unit}{o.sale?' · SALE':''}</small><em title={o.source}><CheckCircle2 size={10}/> {o.observed}</em></>:<span className="missing">—</span>}</td>)}<td className="winner">{best>=0?<><Trophy size={12}/> {STORE_NAMES[best]}</>:'—'}</td></tr>})}</tbody></table></div>}</section>
<div className="freshness">Last verified source check: <b>{data?.meta?.updated_at?new Date(data.meta.updated_at).toLocaleDateString(): '—'}</b> · Blank cells mean no trustworthy current local price has been captured yet.</div>
</main></>}
createRoot(document.getElementById('root')).render(<App/>);